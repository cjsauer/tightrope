(ns splitpea.model)

(def schema {:db/ident       {:db/unique :db.unique/identity}
             ;; :root/new-topic {:db/valueType :db.type/ref}
             :a {}
             :b {}
             :c {}})
